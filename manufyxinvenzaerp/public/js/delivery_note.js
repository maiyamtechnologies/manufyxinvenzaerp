// Delivery Note: finished goods delivered by the piece (sep14 FG plan).
// Owner: A5. Wave 0 placeholder, deliberately empty.
// A5 adds: Qty (Nos) drives the Kg (read-only on FG rows), Nos in the grid, and
// the "Get FG Batches" button.
frappe.ui.form.on("Delivery Note", {});
